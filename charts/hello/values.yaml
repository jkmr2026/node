image:
  repository: myuser/hello
  tag: "latest"
  pullPolicy: Always

service:
  type: ClusterIP
  port: 80

resources: {}

podAnnotations: {}

prometheus:
  enabled: true
  path: /metrics
  port: 9113
